package com.cruz.zoo;

public class Lion extends Animal {
    public Lion(String id, int age, String name, String birthDate, String color,
                String sex, int weight, String sound, String origin, String arrivalDate) {
        super(id, age, name, birthDate, color, sex, weight, sound, origin, arrivalDate);
    }
}
